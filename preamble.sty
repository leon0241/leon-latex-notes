% ╭──────────────────────────────────────────────────────────╮
% │                     General Packages                     │
% ╰──────────────────────────────────────────────────────────╯

% Import Diagrams
\usepackage{import}

% Show frame borders
% \usepackage{showframe}

% Note - this assumes margins are insertd during printing - works with UoE EdPrintPull with margins at 'Minimum'
\usepackage{geometry}
\geometry{
	a4paper,
	margin=0in
}

%Generic space savings
\usepackage{savetrees}

% ╭──────────────────────────────────────────────────────────╮
% │                   Diagrams and Visual                    │
% ╰──────────────────────────────────────────────────────────╯

\usepackage{import}
\usepackage{xifthen}
\usepackage{pdfpages}
\usepackage{transparent}

\newcommand{\incfig}[1]{%
    \import{./figures/}{#1.pdf_tex}
}



% Quiver for Tikz-cd diagrams
\usepackage{quiver}
% Proof Trees
\usepackage{prftree}
% Required for inserting images
\usepackage{graphicx, atbegshi, wrapfig}
%Tell graphicx where to find images
\graphicspath{{./images/}}
% Needed for \mathmakebox for Arrow symbols
\usepackage{mathtools}
% Needed for Fixed Table widths
\usepackage{array}
% Fixed float figure insertion
\usepackage{float}


% ╭──────────────────────────────────────────────────────────╮
% │                 Import Fonts and Symbols                 │
% ╰──────────────────────────────────────────────────────────╯

\usepackage{centernot}

% Needed for variable font sizes
\usepackage{lmodern, anyfontsize}
%\usepackage[final, nopatch=footnote]{microtype} - This package causes significant problems with AMS packages, is purely aesthetical, and is only left commented in in honour of Leon being a massive neek - Louis
% AMS packages. Note: AMSThm for proof env.
\usepackage{amsmath, amsthm, amsfonts, amssymb}
%Fixes maths symbols scalings (int, sqrt, brackets, etc). Must be loaded after lmodern
\usepackage{fixcmex}
% Provides \mathscr font
\usepackage{mathrsfs}
% Provides Bisimulation Symbol
\usepackage{stmaryrd}
% Underscore compatability
\usepackage{underscore}
% bold math
% \usepackage{bm}
% More colours
\usepackage[dvipsnames]{xcolor}
% String manipulation - needed for thmboxes
\usepackage{xstring}
%Smaller bullet point symbol to match \fontsize{4pt}{5pt}
\usepackage{adfbullets}


% ╭──────────────────────────────────────────────────────────╮
% │                   Import Environments                    │
% ╰──────────────────────────────────────────────────────────╯

\usepackage{rotating}

% Multiple columns
\usepackage{multicol}
% Multiple columns (variable width)
\usepackage{vwcol}
% Multiple rows
\usepackage{multirow}
% Subfigures and Subcaptions
\usepackage{subcaption}
% Adds functionality for Enumerate
\usepackage{enumitem}
% Better Tables
\usepackage{tabularx}
% Code blocs
\usepackage{listings}


% ╭──────────────────────────────────────────────────────────╮
% │                     Custom Packages                      │
% ╰──────────────────────────────────────────────────────────╯

% annoying thmboxes issue
\usepackage{silence}
\WarningFilter{latex}{You have requested package}

% MDFramed on Tikz-cd - Required for Thmboxes
\usepackage[framemethod=TikZ]{mdframed}
\usepackage[skins, breakable]{tcolorbox}
% Custom Environments and Objects
\usepackage{../../rss/customs}

% Hyper References
\usepackage{hyperref}
% Better References
\usepackage[capitalise, noabbrev]{cleveref}


% ╭──────────────────────────────────────────────────────────╮
% │                         Options                          │
% ╰──────────────────────────────────────────────────────────╯

\pretolerance=0
\hyphenpenalty=0



\usepackage[nodisplayskipstretch]{setspace}

\parindent = 0pt


%Reduce itemize environment spacing
\setlist{noitemsep, topsep=0pt, parsep=0pt, partopsep=0pt, leftmargin=*, labelsep=1pt}

%Reduce array environment spacing
\setlength\arraycolsep{0pt}


%Smaller bullet point to match \fontsize{4pt}{5pt}
\renewcommand\labelitemi{$\vcenter{\hbox{\tiny\adfbullet{27}}}$}


%Reduce multicols spacing
\setlength{\columnsep}{3pt}


\setlength{\parsep}{0pt}


\lstset{ %this is the stype
mathescape=true,
frame=tb,
columns=fullflexible,
basicstyle=\ttfamily,
keywordstyle=\color{black}\bfseries\em,
keywords={split} %add the keywords you want, or load a language as Rubens explains in his comment above.
morecomment=[l][\color{red}]{#}
}
