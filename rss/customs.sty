% ╭──────────────────────────────────────────────────────────╮
% │                     Custom Commands                      │
% ╰──────────────────────────────────────────────────────────╯

% \newcommand*{\prightarrow}[1]{\xrightarrow{\mathmakebox[6pt]{#1}}}
\newcommand*{\prightarrow}[1]{\xrightarrow{\mathmakebox[15pt]{#1}}}

\def\textrule#1{
    \parbox\textwidth{\hrulefill\hspace{0.5em}\lower.3ex\hbox{\textbf{#1}}\hspace{0.5em}\hrulefill}\vspace{1ex}
    \newline
}

\newcommand\longrule[1]{
    \noindent\rule{\textwidth}{#1}
}

\newcommand\ctl[1]{\mathrm{\mathbf{#1}}}


% ╭──────────────────────────────────────────────────────────╮
% │                  Redefine Environments                   │
% ╰──────────────────────────────────────────────────────────╯

% Set lists to have zero spacing between items to squeeze more in
% Version with zero left bullet padding
\newenvironment{enumerate-zero}{
    \begin{enumerate}[leftmargin=*]
} {\end{enumerate}}

% Alternative versions with alpha numbering
\newenvironment{enumerate-a}{
    \begin{enumerate}[label=\alph*)]
} {\end{enumerate}}

\newenvironment{enumerate-a-zero}{
\begin{enumerate}[leftmargin=*,label=\alph*)]
} {\end{enumerate}}

\newenvironment{itemize-zero}{
    \begin{itemize}[leftmargin=*]
} {\end{itemize}}
